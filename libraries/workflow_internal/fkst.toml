kind = "library"
name = "workflow_internal"

[code]
root = "."

[lib_deps]
libraries = ["contract"]

[dependency_constraints]
allowed_lib_deps = ["contract"]

[library]
name = "workflow_internal"
stable_id = "fkst.library.workflow-internal"
version = "workspace"

[exports]
exact = true
public = [
  "workflow_internal.codex",
  "workflow_internal.env",
  "workflow_internal.liveness.contract",
  "workflow_internal.liveness.shared",
  "workflow_internal.logging",
  "workflow_internal.oracle",
  "workflow_internal.ports",
  "workflow_internal.registry",
  "workflow_internal.restart_liveness_contract",
  "workflow_internal.sweep",
]

[visibility]
public = false

[conformance]
pack = "conformance/pack.toml"

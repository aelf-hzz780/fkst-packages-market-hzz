kind = "library"
name = "contract"

[code]
root = "."

[lib_deps]
libraries = []

[dependency_constraints]
allowed_lib_deps = []

[library]
name = "contract"
publishable = true
stable_id = "fkst.library.contract"
version = "workspace"

[exports]
exact = true
public = ["contract.convergence_identity", "contract.error_facts", "contract.external_pr_bridge", "contract.markdown_fields", "contract.marketing_content", "contract.marketing_schedule", "contract.payload", "contract.session_route", "contract.sha256", "contract.source_ref", "contract.strings", "contract.time", "contract.transition_version", "contract.x_publishing_contract", "contract.x_text"]

[visibility]
public = true

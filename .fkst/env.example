# Copy this file to .fkst/env (gitignored) and fill in local values:
#   cp .fkst/env.example .fkst/env
# scripts/run.sh formal-gate deliberately skips .fkst/env; pass only BIN and, when needed,
# FKST_FRAMEWORK_SOURCE_ROOT through its process environment.

# Path to a locally built fkst-framework binary.
# Example:
# BIN=/path/to/fkst-substrate/target/debug/fkst-framework
BIN=

# The runner requires BIN's source SHA and clean code provenance to match .fkst/substrate-ref.
# Set this only when intentionally testing a different full source SHA.
# FKST_FRAMEWORK_EXPECTED_SHA=0123456789abcdef0123456789abcdef01234567
# Set this for custom target layouts; every run requires matching HEAD and a clean source checkout.
# FKST_FRAMEWORK_SOURCE_ROOT=/path/to/fkst-substrate

# Optional diagnostic source for exporting the official github-proxy package during
# local composed tests. formal-gate ignores both overrides and uses the tracked pin.
# FKST_OFFICIAL_PACKAGE_SOURCE_URL=https://github.com/ChronoAIProject/fkst-hosted.git
# The diagnostic ref must still be a full lowercase commit SHA, never a branch.
# FKST_OFFICIAL_PACKAGE_SOURCE_REF=6fe5f82f76f6b2c02058488587f5f6281c203cf3

# GitHub issue source for local live runs.
# FKST_GITHUB_REPO=owner/repo
# FKST_GITHUB_WRITE=1

# X publishing authority. Keep real values only in .fkst/env or a FKST
# Environment Profile; never commit them.
# X_PUBLISH_WRITE=1
# NYXID_URL=https://nyxid.example.com
# NYXID_X_SERVICE_SLUG=x-service-slug
# X_PUBLISH_EXPECTED_USERNAME=expected-x-username
# X_PUBLISH_NATIVE_QUOTE=1
# NYXID_ACCESS_TOKEN=nyxid-agent-access-token

# LLM runtime configuration, if the host/session requires it.
# LLM_API_KEY=provider-key
# LLM_BASE_URL=https://provider.example.com/v1
# LLM_MODEL=model-name

# Runtime roots. Defaults are under ignored .fkst/run when unset.
# FKST_RUNTIME_ROOT=/path/to/runtime
# FKST_DURABLE_ROOT=/path/to/durable
# FKST_RATE_POOL_ROOT=/path/to/rate-pools

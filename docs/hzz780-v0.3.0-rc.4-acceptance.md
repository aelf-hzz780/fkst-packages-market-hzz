# hzz780 v0.3.0-rc.4 product acceptance runbook

This runbook validates the Auto Twitter v0.3.0 review workflow with isolated copies of the COO
Signals and the `hzz780` account. It authorizes GitHub Issue, comment, and Close writes required by
the workflow, but it never authorizes a Schedule or an X write.

本文使用 COO Signal 的隔离副本与 `hzz780` 账号验收 Auto Twitter v0.3.0 review workflow。
验收允许流程所需的 GitHub Issue、comment 与 Close 写入，但绝不授权创建 Schedule 或写入 X。

> **Not ready to run yet / 尚不可执行：** `v0.3.0-rc.4` does not exist until the tested commit is
> pushed, CI is green, and the tag is created. Do not substitute a branch or an earlier RC tag.
>
> `v0.3.0-rc.4` 只有在测试 commit 已 push、CI 全绿并创建 tag 后才存在。在此之前不得用 branch
> 或更早的 RC tag 代替。

## Scope and release boundary / 范围与发布边界

| Item / 项目 | Acceptance value / 验收值 |
|---|---|
| Repository | `aelf-hzz780/fkst-packages-market-hzz` |
| Source evidence | Existing Issues `#116/#117/#118/#124`, read-only / 只读 |
| X identity | `hzz780` |
| Session creator and reviewer | `aelf-hzz780` |
| Stable logical work label | `auto-x-hzz780` |
| Business descriptor | `aelf-hzz780/fkst-packages-market-hzz@v0.3.0-rc.4:manifests/auto-twitter-marketing.json` |
| Official adapter descriptor | `ChronoAIProject/fkst-hosted@packages:packages/github-proxy` |
| X write | Forbidden / 禁止 |

The official adapter branch is a Shadow-only exception. Hosted cannot clone a raw commit SHA and
the official repository has no immutable tag containing `packages/github-proxy`. Local and CI
composition is pinned to official commit `6fe5f82f76f6b2c02058488587f5f6281c203cf3` and tree
`sha256-7cc28b7058c0155b459304f68129e9c778de57a3ed3ae87e017794c55eef5fe6`. The branch must resolve
to that commit immediately before each Trigger starts. Branch drift stops the acceptance. Stable
production remains blocked until an official immutable ref exists or a separate Host solution is
agreed; this runbook does not authorize a Host change.

Do not start a Stable Hosted Session while stable_hosted_release=blocked.

Official adapter branch 只是 Shadow 例外。Hosted 当前不能 clone raw commit SHA，official repo
也没有包含 `packages/github-proxy` 的 immutable tag。本地与 CI composition 固定在 official
commit `6fe5f82f76f6b2c02058488587f5f6281c203cf3` 和 tree
`sha256-7cc28b7058c0155b459304f68129e9c778de57a3ed3ae87e017794c55eef5fe6`。每个 Trigger 启动前，
branch 都必须解析到该 commit；发生 drift 立即停止验收。正式生产仍需 official immutable ref，
或另行确认 Host 方案；本 Runbook 不授权修改 Host。

## Formal gate / 正式门禁

Run the following from a clean checkout of the RC4 commit. The framework binary and source checkout
must match `.fkst/substrate-ref`, and both binary and checkout provenance must be clean.

在 RC4 commit 的干净 checkout 中执行以下门禁。Framework binary 与源码 checkout 必须匹配
`.fkst/substrate-ref`，且 binary、checkout provenance 都必须为 clean。

```bash
scripts/run.sh formal-gate
```

Required results are contract checker and runner checks all passing, package suites all passing,
composed conformance `15/15`, graph `20 departments / 20 queues`, and the official tree digest above.
The RC4 tag must resolve to the exact tested commit and every business package reference in the
manifest must use that same tag.

The gate requires the business repository to have a clean tracked and untracked worktree before and
after the test suites. Both checks emit `RELEASE_PROVENANCE_CHECK_OK` with the package ref, tested
HEAD, release context, and tag commit. A
branch/PR run attests the clean HEAD without requiring a future tag. Pushing `v0.3.0-rc.4` triggers
the same workflow again in strict tag mode; that run must prove
`manifest ref == GitHub tag == tag commit == tested HEAD` before Shadow rollout.

The command must show the four stages in order and emit the tracked/effective official URL/ref,
fetched official SHA, and locked official tree. It skips `.fkst/env` and fails if the process
environment defines a framework expected-SHA or official URL/ref override. Diagnostic runs with an
alternate framework do not satisfy this gate. The checker must also report
`hosted_ref_class=mutable-shadow-only` and `stable_hosted_release=blocked`; this is expected for the
Shadow and remains a Stable release blocker.

必须满足：contract checker 与 runner checks 全部通过、三个 package suite 全部通过、composed
conformance `15/15`、graph `20 departments / 20 queues`，且 official tree digest 与上文一致。
RC4 tag 必须指向同一个已测试 commit，Manifest 内所有 business package ref 也必须使用该 tag。
门禁在测试前后都要求 business repository 的 tracked 与 untracked worktree 均为 clean；两次
检查都会输出包含 package ref、tested HEAD、release context 与 tag commit 的
`RELEASE_PROVENANCE_CHECK_OK`。Branch/PR
阶段只证明 clean HEAD，不要求尚未创建的未来 tag；push `v0.3.0-rc.4` 后，同一个 workflow 会在
strict tag mode 再跑一次，且必须证明
`manifest ref == GitHub tag == tag commit == tested HEAD`，之后才能开始 Shadow。
命令必须按顺序显示四个 stage，并输出 tracked/effective official URL/ref、实际 fetch official
SHA 与 locked official tree。它跳过 `.fkst/env`；process environment 只要定义 framework
expected-SHA 或 official URL/ref override 就会 fail closed。Alternate framework diagnostic 不能
满足本门禁。Checker 还必须输出 `hosted_ref_class=mutable-shadow-only` 与
`stable_hosted_release=blocked`；这是 Shadow 的预期状态，同时仍是 Stable 发布阻塞项。

Fault injection stays in the mocked formal gate. Hosted acceptance does not manually forge Bot
markers or force partial GitHub failures.

故障注入只在 mock 正式门禁中验证；Hosted 验收不手工伪造 Bot marker，也不强制制造 GitHub
partial failure。

| Formal-only case / 仅正式测试场景 | Required proof / 必须证明 |
|---|---|
| First draft over 280 weighted characters | One bounded correction, then valid output or durable triage |
| Partial Signal-set ACK | Non-anchor ACK cannot freeze the set; anchor repairs missing sibling ACKs |
| Lost comment or Close response | Fresh read converges without duplicate artifact |
| Missing, forged, or duplicate Bot marker | Review command does not retire incorrectly |
| REST versus GraphQL comment identity | Only the fresh numeric REST comment ID is authoritative |
| Malformed Proposal controls | Catalog fails closed and does not create a duplicate Proposal |
| Dirty/untracked official cache | Exact-SHA `git archive` export excludes local cache changes |

## Environment and account preflight / 环境与账号预检

Use the reusable Environment Profile `hzz780-x-shadow`. Validate it without printing credentials:

使用 reusable Environment Profile `hzz780-x-shadow`，并在不打印 credential 的前提下确认：

```text
X_PUBLISH_EXPECTED_USERNAME=hzz780
X_PUBLISH_WRITE=0
FKST_X_PUBLISH_WRITE=0
NYXID_X_SERVICE_SLUG=<the hzz780 service>
FKST_GITHUB_BOT_LOGIN=<registered trusted bot login>
```

The NyxID service must return `hzz780` from `/users/me`. `FKST_GITHUB_WRITE=1`,
`FKST_SESSION_CREATOR=aelf-hzz780`, `FKST_GITHUB_BOT_LOGIN`, the effective work-label mapping, and
`FKST_GITHUB_AUTHORIZED_LOGINS` are Host-owned values; do not place them in Package Env. Verify that
the bot login is present, matches the registered trusted app identity, and is the author of the
Proposal/Content artifacts. Verify from the Trigger registration and observed workflow behavior that
GitHub writes are enabled, the creator is `aelf-hzz780`, the logical label resolves to
`auto-x-hzz780`, and reviews by `aelf-hzz780` are authorized. Any conflicting account, creator, or
bot login is a blocker.

NyxID `/users/me` 必须返回 `hzz780`。`FKST_GITHUB_WRITE=1`、
`FKST_SESSION_CREATOR=aelf-hzz780`、`FKST_GITHUB_BOT_LOGIN`、effective work-label mapping 与
`FKST_GITHUB_AUTHORIZED_LOGINS` 都由 Host 管理，不得写入 Package Env。必须确认 bot login 已注入、
与注册的 trusted app identity 一致，并且 Proposal/Content 均由该 bot 创建；再通过 Trigger
registration 与实际流程结果确认 GitHub write 已开启、creator 为 `aelf-hzz780`、逻辑 label
可反解为 `auto-x-hzz780`，且 `aelf-hzz780` 的审核命令被授权。账号、creator 或 bot login 冲突时
立即停止。

Before any GitHub write, perform one read-only identity request through NyxID using the exact service
slug and NyxID identity selected by `hzz780-x-shadow`:

```bash
nyxid proxy request <hzz780-service-slug> \
  '/users/me?user.fields=id,name,username' -m GET
```

Record `Tidentity` in UTC, the non-secret service slug, and only a redacted minimal response such as
`{"data":{"username":"hzz780"}}`; omit the user id and display name, and never record a NyxID token or
downstream credential. A failed request, an ambiguous service binding, or any username other than
`hzz780` is a pre-write blocker.

在产生任何 GitHub 写入前，必须使用 `hzz780-x-shadow` 选中的同一 NyxID identity 与 exact service
slug，通过上述 NyxID CLI 命令执行一次只读身份请求。证据只记录 UTC `Tidentity`、非敏感的 service
slug，以及形如 `{"data":{"username":"hzz780"}}` 的最小脱敏响应；省略 user id 与 display name，
绝不记录 NyxID token 或下游 credential。请求失败、service binding 有歧义或 username 不是
`hzz780` 都是 pre-write blocker。

### Hosted observability blocker / Hosted 可观测性阻塞

Before recording `T0` or making any RC4 GitHub write, verify Hosted health and log storage through a
known retained non-RC4 probe Session that the operator is authorized to inspect. Use the authenticated
surfaces below with one concrete `<probe-run-id>` returned by the runs endpoint:

```text
GET /api/v1/sessions/<probe-session-id>/health
GET /api/v1/logs/<probe-session-id>/runs
GET /api/v1/logs/<probe-session-id>/manifest?run=<probe-run-id>
GET /api/v1/logs/<probe-session-id>?run=<probe-run-id>
```

All four requests must return usable data: health contains a parseable retained report, runs contains
the selected run, manifest describes that run, and bundle download returns a non-empty gzip. Record
the UTC probe time, probe Session/run IDs, HTTP statuses, manifest, and bundle SHA-256, but no bearer
token. A `404`, `502`, `503`, empty/corrupt bundle, or inconsistent run/manifest is a blocker. The RC3
postmortem last observed both of these responses:

```json
{"error":"unavailable","message":"health storage is not configured"}
{"error":"unavailable","message":"log storage is not configured"}
```

An Observe snapshot alone is not an acceptable substitute because the acceptance requires historical
run records and redacted logs to prove zero X queue delivery and zero `/tweets` POST. If any probe
fails, stop with zero RC4 GitHub writes and escalate the Hosted environment blocker; do not close an
older Trigger, create fixtures, or create Trigger A, and do not weaken the evidence gate.

在记录 `T0` 或产生任何 RC4 GitHub 写入前，必须使用运营人员有权读取的一条已留存非 RC4 probe
Session，通过上述 authenticated endpoint 验证 Hosted health 与 log storage。先从 runs endpoint
取得一个 concrete `<probe-run-id>`；四个请求必须全部返回可用数据：health 含可解析的留存报告、
runs 包含所选 run、manifest 对应该 run、bundle 是非空 gzip。证据记录 UTC probe 时间、probe
Session/run ID、HTTP status、manifest 与 bundle SHA-256，但不记录 bearer token。任何 `404`、
`502`、`503`、空/损坏 bundle 或 run/manifest 不一致都直接阻塞。RC3 复盘时最后观测到的结果正是
上面两条 `storage is not configured`。

Observe snapshot 不能替代这些证据，因为本验收必须用历史 run record 与脱敏日志证明零 X queue
delivery 和零 `/tweets` POST。任一 probe 失败时，必须在产生任何 RC4 GitHub 写入前停止，将其作为
Hosted 环境 blocker 升级处理；不得关闭旧 Trigger、创建 fixture/Trigger A，也不得降低证据门禁。

### Prior batch isolation / 历史批次隔离

The RC2 negative fixtures `#135/#136` and failed RC3 Signals `#140/#141/#142/#143` are immutable
historical evidence. They must remain outside the RC4 route and must never be relabeled, edited,
reopened, commented on, or otherwise replayed for this acceptance. RC3 Trigger `#144` must remain
Closed. The valid closed RC2 Proposals `#131/#132` must also remain untouched; RC4 is expected to
classify their exact legacy create markers as completed history while still failing closed for an
Open or malformed legacy review.

RC2 负测 fixture `#135/#136` 与失败的 RC3 Signal `#140/#141/#142/#143` 是不可变历史证据。它们
必须始终位于 RC4 route 之外，本轮验收不得重新加 label、修改、reopen、comment 或以其他方式
重放。RC3 Trigger `#144` 必须保持 Closed。合法且已 Closed 的 RC2 Proposal `#131/#132` 也不得
修改；RC4 应把其中精确的 legacy create marker 识别为已完成历史，同时继续对 Open 或 malformed
legacy review fail closed。

Reusing `#140/#141/#142/#143` would reuse their RC3 pickup, triage, and retirement markers, so it
would not exercise fresh ingress or prove the migration fix. RC4 must create four new copies from the
read-only source Issues `#116/#117/#118/#124` and scope every result to those new Issue IDs.

复用 `#140/#141/#142/#143` 会复用其 RC3 pickup、triage 与 retirement marker，既不能覆盖 fresh
ingress，也不能证明迁移修复。RC4 必须从只读来源 Issue `#116/#117/#118/#124` 重新创建四份副本，
并用这些全新 Issue ID 限定所有验收结果。

After the observability and NyxID gates pass, close every older Trigger that owns `auto-x-hzz780` and
wait until its runtime is gone. Then capture the authenticated Session registry and record its UTC
timestamp as `Tsession-zero`:

```text
GET /api/v1/repos/aelf-hzz780/fkst-packages-market-hzz/sessions
```

Filter both `work_label` and `work_labels`. There must be zero Session whose effective labels include
`auto-x-hzz780` and whose runtime is live, starting, recovering, degraded, or otherwise not
unambiguously absent. An unknown or ambiguous runtime blocks the run. Save the registry response as
pre-fixture evidence; an Open-Issue query cannot substitute for runtime proof.

Also query all Open Issues carrying the label. The historical Issues above must not appear; if any
unexpectedly carries the routing label, stop and investigate before changing state. The pre-fixture
query must return zero Open work Issues.

Observability 与 NyxID 门禁通过后，先关闭所有占用 `auto-x-hzz780` 的旧 Trigger，并等待对应
runtime 消失；随后调用上述 authenticated Session registry，并把 UTC 时间记录为
`Tsession-zero`。同时检查 `work_label` 与 `work_labels`：effective label 包含
`auto-x-hzz780`，且 runtime 处于 live、starting、recovering、degraded 或其他不能明确证明 absent
状态的 Session 必须严格为零；unknown/ambiguous 状态直接阻塞。保存 registry response 作为
pre-fixture 证据，Open Issue 查询不能替代 runtime 证明。

还要查询所有携带该 label 的 Open Issue。上述历史 Issue 不得出现在结果中；如果其中任一意外携带
routing label，必须先停止并调查，不能直接改变状态。准备新 fixture 前，该查询必须返回零条 Open
work Issue。

```bash
gh issue list --repo aelf-hzz780/fkst-packages-market-hzz \
  --state open --label auto-x-hzz780 --limit 100 \
  --json number,title,labels,assignees
```

Record the RC4 commit, tag resolution, official branch resolution, profile name, and the start time
`T0`. Never record tokens, service credentials, or authorization headers.

记录 RC4 commit、tag resolution、official branch resolution、Profile 名称与开始时间 `T0`；
禁止记录 token、service credential 或 authorization header。

## COO fixture construction / COO Fixture 构造

Prepare all four copies while no Session owns `auto-x-hzz780`. This is load-bearing: if copies are
created one by one under a live Session, the first W33 Signal can be proposed before the other two
exist, so the run no longer validates three-Signal aggregation.

在没有 Session 占用 `auto-x-hzz780` 时一次性准备四份副本。这是关键前提：若在 live Session
中逐条创建，第一条 W33 Signal 可能在另外两条出现前就先生成 Proposal，无法真实验收三条
Signal 聚合。

For each source Issue, copy the complete title and body. Replace controls in the original header in
place; never prepend another control block. Preserve every other field, the complete COO narrative,
all evidence URLs, and the original `loop-critic` HTML marker. The marker may still say
`aelfblockchain`; it is source evidence, not route authority. Add one narrative line immediately
after the control header: `Acceptance copy of: <source Issue URL>`.

每个来源 Issue 都完整复制 title 与 body，并在原 header 中原位替换 controls，禁止在顶部追加
第二份 control block。其余字段、COO 完整叙事、全部 evidence URL 与原 `loop-critic` HTML marker
原样保留。Marker 中仍出现 `aelfblockchain` 是正常的，它属于来源证据，不是 route authority。
在 control header 后追加一行叙事：`Acceptance copy of: <source Issue URL>`。

Every copy must contain these authoritative controls exactly once:

每份副本必须且只能出现一次以下权威 controls：

```yaml
contract: marketing-radar.radar-signal.v2
type: radar-signal
project: aelf-x-ops
account: hzz780
work-label: auto-x-hzz780
week: <mapped week>
action: add
topic: <mapped canonical topic>
```

Keep the source `date`, `source-url`, `insight`, `priority`, `competitors`, `position`, and
`content-gap-days` values unchanged. Apply this fixed mapping:

来源中的 `date`、`source-url`、`insight`、`priority`、`competitors`、`position` 与
`content-gap-days` 保持不变，固定映射如下：

| Source / 来源 | Week | Canonical topic | Acceptance title prefix |
|---|---|---|---|
| `#116` | `2026-W33` | `publishing-gap` | `[RC4 Shadow copy #116]` |
| `#117` | `2026-W33` | `publishing-gap` | `[RC4 Shadow copy #117]` |
| `#118` | `2026-W33` | `publishing-gap` | `[RC4 Shadow copy #118]` |
| `#124` | `2026-W34` | `content-supply-gap` | `[RC4 Shadow copy #124]` |

Create each copy as `aelf-hzz780`, assign only `aelf-hzz780`, and apply exactly the routing label
`auto-x-hzz780`. Do not create a Schedule. Record the four new Issue numbers as `S116`, `S117`,
`S118`, and `S124`. After all four exist, query the route and require exactly those four Issues.

四份副本都由 `aelf-hzz780` 创建，唯一 assignee 为 `aelf-hzz780`，routing label 为
`auto-x-hzz780`。不得创建 Schedule。将四个新 Issue number 记录为 `S116/S117/S118/S124`。
全部创建完成后查询 route，结果必须严格等于这四条。

## Trigger bodies / Trigger 正文

Immediately before creating each Trigger, run the following and record the result as `Tpkg-pre-A`
or `Tpkg-pre-B`. After that Trigger is registered and its runtime is live, run it again and record
`Tpkg-post-A` or `Tpkg-post-B` before accepting any workflow output or issuing any review command.

每个 Trigger 创建前立即执行以下命令，并把结果记录为 `Tpkg-pre-A` 或 `Tpkg-pre-B`。Trigger
完成 registration 且 runtime live 后再次执行，记录为 `Tpkg-post-A` 或 `Tpkg-post-B`；在完成
post-check 前，不得把任何 workflow output 计入证据，也不得提交 review command。

```bash
git ls-remote https://github.com/ChronoAIProject/fkst-hosted.git refs/heads/packages
```

All four values must be `6fe5f82f76f6b2c02058488587f5f6281c203cf3`. Start Trigger A with this exact body:

四次结果都必须为 `6fe5f82f76f6b2c02058488587f5f6281c203cf3`。Trigger A 使用以下完整正文：

```markdown
### Session Name
auto-x-hzz780-rc4-a

### Environment
hzz780-x-shadow

### Auto-merge
false

### Work Label
auto-x-hzz780

### Engine Config
FKST_LLM_REASONING_EFFORT=high

### Packages
ChronoAIProject/fkst-hosted@packages:packages/github-proxy

### Manifest
aelf-hzz780/fkst-packages-market-hzz@v0.3.0-rc.4:manifests/auto-twitter-marketing.json
```

The Trigger must be authored by `aelf-hzz780` and carry `fkst-substrate-trigger`. Do not set
Package Env, collaborators, Auto-merge, X write, or a Source/Target branch beyond the values shown.
Save the Trigger URL, registration comment, Session ID, config hash, effective labels, and log URL.

Trigger 必须由 `aelf-hzz780` 创建并携带 `fkst-substrate-trigger`。除正文所示配置外，不设置
Package Env、collaborator、Auto-merge、X write 或 Source/Target branch。保存 Trigger URL、
registration comment、Session ID、config hash、effective labels 与 log URL。

Trigger B uses the identical body except `### Session Name` is `auto-x-hzz780-rc4-b`. Trigger B may
start only after Trigger A is closed and A's runtime is confirmed absent.

Trigger B 使用完全相同的正文，只有 `### Session Name` 改为 `auto-x-hzz780-rc4-b`。只有在
Trigger A 已关闭且其 runtime 确认消失后，才能启动 Trigger B。

If either post-check differs from its pre-check or from the pinned SHA, immediately stop review
actions, save the Trigger registration, current Observe snapshot, redacted logs, and both SHA
observations, then close the active Trigger and wait for its runtime to disappear. Remove
`auto-x-hzz780` from every still-Open Signal, Proposal, Content, and negative fixture created in this
batch. Freeze terminal artifacts as invalid evidence; do not delete or reuse them. The entire batch
is void even if its counts look correct. Restart from a new `T0` with four newly copied COO Signals
and new Trigger A/B records.

任一 post-check 与对应 pre-check 或 pinned SHA 不一致时，立即停止 review action，保存 Trigger
registration、当前 Observe snapshot、脱敏日志以及两次 SHA 观测；随后关闭 active Trigger 并等待
runtime 消失。从本批次所有仍 Open 的 Signal、Proposal、Content 与 negative fixture 移除
`auto-x-hzz780`。已终态 artifact 只冻结为无效证据，不删除、不复用。即使数量看似正确，本批次
仍整体判废；必须使用新的 `T0`、四份全新 COO Signal 副本以及新的 Trigger A/B 记录从头执行。

These double checks authorize only this RC4 Shadow. They do not make the mutable official branch a
Stable Production pin. Stable remains blocked until the official package has an immutable tag/ref,
or a separately reviewed Host raw-SHA solution is approved.

上述双查只授权本次 RC4 Shadow，不会把 mutable official branch 变成 Stable Production pin。
在 official package 提供 immutable tag/ref，或另行评审并批准 Host raw-SHA 方案前，Stable 仍被
阻塞。

## Product acceptance sequence / 产品验收步骤

### Phase A: aggregate and draft / 聚合与草稿

1. After all four copies exist, record `Tpkg-pre-A`, start Trigger A, wait for registration and a live
   runtime, then record `Tpkg-post-A`. Capture the Session registry as `TA-owner`; after filtering
   both `work_label` and `work_labels`, the active `auto-x-hzz780` owner set must be exactly
   `{<session-a-id>}`. Apply the drift procedure above before continuing if either SHA is not pinned,
   and stop for any missing, duplicate, or ambiguous owner.
2. Only after `Tpkg-post-A` passes, poll every 30 seconds. Routing ACK P90 is 5 minutes and draft P90
   is 10 minutes; the hard timeout is 20 minutes. Do not edit a Signal body or add a replay comment
   while waiting.
3. Require exactly two new `weekly-plan-change` Proposals in the `T0` window:
   - W33 contains exactly `S116/S117/S118` in its `signal:` lineage;
   - W34 contains exactly `S124`;
   - no Proposal references `#135/#136`, `#140/#141/#142/#143`, or any other prior acceptance Issue.
4. Record each Proposal URL, `proposal-id`, revision, `signal-set-digest`, `proposal-digest`,
   `content-digest`, exact `tweet-text`, and `signal:` lines. A generated Proposal proves its text
   passed the package's deterministic X weighted-length contract (`<=280`); first-attempt/correction
   behavior is formal test evidence, not a Hosted assertion.
5. Require zero new `weekly-content`, zero `schedule-publish`, and no
   `x-publisher.x_publish_request` or `x-publisher.x_published` delivery in Session A.

1. 四份副本全部存在后，记录 `Tpkg-pre-A`，启动 Trigger A；等待 registration 完成且 runtime
   live 后记录 `Tpkg-post-A`，并把 Session registry 保存为 `TA-owner`。同时过滤 `work_label` 与
   `work_labels` 后，active `auto-x-hzz780` owner 集合必须严格等于 `{<session-a-id>}`。任一 SHA
   不是 pinned 值时执行上文 drift 处置；owner 缺失、重复或有歧义时立即停止。
2. `Tpkg-post-A` 通过后才开始每 30 秒轮询一次。Routing ACK P90 为 5 分钟，draft P90 为 10
   分钟，hard timeout 为 20 分钟；等待期间不修改 Signal body，也不添加 replay comment。
3. 在 `T0` 时间窗内必须严格只有两个新 `weekly-plan-change` Proposal：
   - W33 的 `signal:` lineage 严格为 `S116/S117/S118`；
   - W34 的 lineage 严格为 `S124`；
   - 两者都不得引用 `#135/#136`、`#140/#141/#142/#143` 或其他历史验收 Issue。
4. 记录两个 Proposal URL、`proposal-id`、revision、`signal-set-digest`、`proposal-digest`、
   `content-digest`、完整 `tweet-text` 与 `signal:` lines。Proposal 能生成即证明文案已通过
   package 的确定性 X weighted-length contract (`<=280`)；是否首轮生成或触发 correction 由
   正式测试证明，不作为 Hosted 人工断言。
5. Session A 内必须为零 `weekly-content`、零 `schedule-publish`，且没有
   `x-publisher.x_publish_request` 或 `x-publisher.x_published` delivery。

### Phase B: sequential Session takeover / 顺序 Session 接管

6. Capture Session A logs and a final current-state Observe snapshot, then close Trigger A. Poll the
   Session registry until the active `auto-x-hzz780` owner set is strictly empty. Also require
   `GET /api/v1/sessions/<session-a-id>/observe?limit=10000` to return `404` with the fixed
   no-live-runtime meaning. Save both responses and their UTC time as `TA-absent`, then refresh A's
   run list, manifests, and bundles so its retained history covers shutdown. A `403`, `409`, `503`, or
   ambiguous registry state is not absence. The four Signals and two Proposals stay Open; a retirement
   label is not a terminal business decision.
7. Only after `TA-absent` is recorded, record `Tpkg-pre-B`, start Trigger B with the body above, wait
   for registration and a live runtime, then record `Tpkg-post-B`. Capture the Session registry as
   `TB-owner`; the filtered active owner set must be exactly `{<session-b-id>}`. Apply the drift
   procedure before continuing if either SHA is not pinned, and stop for any missing, duplicate, or
   ambiguous owner. Do not relabel, edit, reopen, or comment on the six work Issues to force replay.
8. After quiescence, require the same two Proposal IDs, revisions, Signal-set digests, Proposal
   digests, Content digests, and lineage. There must still be exactly two Proposals and zero
   Content/Schedule Issues in the RC4 scope.

6. 保存 Session A logs 与最终 current-state Observe snapshot，然后关闭 Trigger A。轮询 Session
   registry，直到 active `auto-x-hzz780` owner 集合严格为空；同时要求
   `GET /api/v1/sessions/<session-a-id>/observe?limit=10000` 返回具有固定 no-live-runtime 语义的
   `404`。把两份响应与 UTC 时间记录为 `TA-absent`，随后刷新 A 的 run list、manifest 与 bundle，
   确保留存历史覆盖 shutdown。`403`、`409`、`503` 或 ambiguous registry 状态都不代表 absent。
   四个 Signal 与两个 Proposal 继续保持 Open；Session retirement label 不代表业务终态。
7. 只有记录 `TA-absent` 后，才记录 `Tpkg-pre-B` 并按上文正文启动 Trigger B；等待 registration
   完成且 runtime live 后记录 `Tpkg-post-B`，并把 Session registry 保存为 `TB-owner`。过滤后的
   active owner 集合必须严格等于 `{<session-b-id>}`。任一 SHA 不是 pinned 值时执行 drift 处置；
   owner 缺失、重复或有歧义时立即停止。不得通过 relabel、修改、reopen 或 comment 六个工作
   Issue 来强制 replay。
8. 系统静默后，两个 Proposal 的 ID、revision、Signal-set digest、Proposal digest、Content digest
   与 lineage 必须完全不变。RC4 scope 内仍然严格为两个 Proposal、零 Content、零 Schedule。

### Phase C: simulated COO review / 模拟 COO 审核

9. As `aelf-hzz780`, comment on W33 revision 1:

   ```text
   /marketing request-changes <w33-proposal-id>@1 <w33-r1-proposal-digest> Cite the original evidence and make the CTA concrete.
   ```

10. Wait for revision 2 on the same Proposal and verify no Content exists. Then submit the stale
    approval below and require a visible stale-revision rejection with no Content:

    ```text
    /marketing approve <w33-proposal-id>@1 <w33-r1-proposal-digest>
    ```

11. Approve the latest W33 revision and W34 revision 1:

    ```text
    /marketing approve <w33-proposal-id>@2 <w33-r2-proposal-digest>
    /marketing approve <w34-proposal-id>@1 <w34-r1-proposal-digest>
    ```

12. Without adding replay comments, wait for exactly two immutable approved `weekly-content`
    Issues. Each Content must match the approved Proposal's account, work label, week, approval ID,
    content digest, and exact tweet text.
13. Wait for both Proposals and all four Signal copies to receive terminal status ACKs and close
    after fresh reads. Each Signal Close must also fresh-read its Review Issue and revalidate the
    exact approval comment, revision, Proposal digest, and Content digest. Both Content Issues must
    close after their durable import ACK. A Close that requires an operator replay is a failure.

9. 使用 `aelf-hzz780` 对 W33 revision 1 提交上述 request-changes 命令。
10. 等待同一个 Proposal 生成 revision 2，并确认仍无 Content；随后提交对 revision 1 的 stale
    approve，必须出现可见的 stale-revision 拒绝，且仍不创建 Content。
11. 按上述命令批准 W33 revision 2 与 W34 revision 1。
12. 不添加 replay comment，等待严格两个 immutable、approved `weekly-content` Issue。每条
    Content 的 account、work label、week、approval ID、content digest 与 tweet text 都必须匹配
    对应的 approved Proposal。
13. 等待两个 Proposal 与四个 Signal 副本在 fresh read 后收到 terminal status ACK 并自动关闭；
    每次关闭 Signal 还必须 fresh-read 对应 Review Issue，并重新校验精确的 approval comment、
    revision、Proposal digest 与 Content digest。两个 Content 也必须在 durable import ACK 后自动
    关闭。任何依赖人工 replay 才完成的 Close 都判定失败。

### Phase D: isolated negative cases / 隔离负测

14. Under Trigger B, create two fresh, separately scoped Signal fixtures:
    - account mismatch: `account: aelfblockchain`, otherwise valid `add` controls;
    - invalid target: `account: hzz780`, `action: add`, and `target-ref: <W33 Proposal ref>`.
15. Both must remain Open with visible `needs-triage`, `publish_attempted: false`, no Proposal or
    Content lineage, and no X queue delivery. Record their URLs, then remove the routing label after
    evidence capture so they cannot become future Session work.

14. 在 Trigger B 下创建两条全新且独立 scope 的 Signal 负测：
    - account mismatch：`account: aelfblockchain`，其余为合法 `add` controls；
    - invalid target：`account: hzz780`、`action: add`，并设置
      `target-ref: <W33 Proposal ref>`。
15. 两条都必须保持 Open，留下可见 `needs-triage` 与 `publish_attempted: false`，不产生 Proposal
    或 Content lineage，也不产生 X queue delivery。保存 URL 后移除 routing label，避免它们成为
    后续 Session 的工作项。

### Phase E: final shutdown and historical proof / 最终停机与历史证明

16. After all business and negative-case evidence is captured, wait for Trigger B to become
    quiescent and save its final live Observe snapshot. Close Trigger B, then poll the Session
    registry until the active `auto-x-hzz780` owner set is strictly empty. Require
    `GET /api/v1/sessions/<session-b-id>/observe?limit=10000` to return the same fixed
    no-live-runtime `404`. Save the registry response, `404`, and UTC time as `TB-absent`.
17. After `TB-absent`, refresh B's complete run list, every manifest, and every redacted bundle.
    Re-run the historical X scan over the final A/B evidence set from `T0` through each Session's
    shutdown. Any missing run, time gap, unreadable bundle, X queue delivery, or `/tweets` POST
    attempt fails RC4. Only this shutdown-complete evidence set may be used for the final pass table.

16. 所有业务与负测证据保存完成后，等待 Trigger B 静默并保存最后一份 live Observe snapshot；
    随后关闭 Trigger B，轮询 Session registry，直到 active `auto-x-hzz780` owner 集合严格为空。
    `GET /api/v1/sessions/<session-b-id>/observe?limit=10000` 还必须返回同样具有固定
    no-live-runtime 语义的 `404`。把 registry response、`404` 与 UTC 时间保存为 `TB-absent`。
17. 记录 `TB-absent` 后，刷新 B 的完整 run list、每个 manifest 与每个脱敏 bundle；再用最终
    A/B 证据集扫描从 `T0` 到各自 shutdown 的完整历史。任何 run 缺失、时间断档、bundle 不可读、
    X queue delivery 或 `/tweets` POST attempt 都使 RC4 失败。只有这份覆盖 shutdown 的最终证据集
    可以用于填写通过表。

## Observation and evidence / 观测与证据

Use the dashboard Status tab or the authenticated endpoint below every 30 seconds while a Session is
live. Use `limit=10000` for the final snapshots.

Session live 期间，每 30 秒通过 dashboard Status tab 或以下 authenticated endpoint 观测一次；
最终 snapshot 使用 `limit=10000`。

```text
GET /api/v1/sessions/<session-id>/observe?limit=10000
```

An Observe response is a current delivery-store snapshot, not historical evidence. At quiescence,
each Session's final Observe must show every queue with depth, in-flight, and retrying counts of zero,
and permanent dead letters empty. If `github-proxy.github_pr_changed` is present in a live snapshot,
its delivery must converge through `github-auto-twitter-marketing.optional_pr_event_sink`. Because
ACKed deliveries are removed from Observe, an empty final snapshot proves current convergence only;
it does not prove that an X queue delivery never occurred.

Historical X isolation is a separate `T0`-scoped gate. Enumerate every run for Sessions A and B with
`GET /api/v1/logs/<session-id>/runs`, save every run manifest and redacted bundle, and inspect the full
set from `T0` through shutdown. Across those retained run records and logs there must be zero
`x-publisher.x_publish_request`, zero `x-publisher.x_published`, and zero X `/tweets` POST attempt.
Missing runs, a bundle that cannot be read, truncation, or a time gap makes the historical claim
unprovable and fails RC4; it must never be replaced by an empty Observe snapshot.

Build a final run inventory from each Session's post-shutdown `/runs` response. Every row records
`session_id`, `run_id`, start/end time, manifest SHA-256, and bundle SHA-256. Every listed run must
have a matching manifest and a readable, untruncated redacted bundle. Record both the per-run matches
and aggregate counts for `x_publish_request`, `x_published`, and `POST /tweets`; all three aggregate
counts must be zero. If the `/runs` set changes while manifests or bundles are downloaded, refresh
the inventory and repeat the scan until the set is stable. The final coverage end must be at or after
`TB-absent`.

Observe response 是当前 delivery-store snapshot，不是历史证据。系统静默后，每个 Session 的最终
Observe 必须显示所有 queue 的 depth、in-flight 与 retrying 均为 0，permanent dead letter 为空；
live snapshot 中若出现 `github-proxy.github_pr_changed`，其 delivery 必须通过
`github-auto-twitter-marketing.optional_pr_event_sink` 收敛。由于 ACKed delivery 会从 Observe
移除，最终空 snapshot 只能证明当前已收敛，不能证明历史上从未出现 X queue delivery。

历史 X 隔离是另一道按 `T0` 限定的门禁。通过 `GET /api/v1/logs/<session-id>/runs` 枚举 Session
A/B 的每个 run，保存所有 run manifest 与脱敏 bundle，并检查从 `T0` 到 shutdown 的完整集合。
留存 run record 与日志中必须为零 `x-publisher.x_publish_request`、零
`x-publisher.x_published`、零 X `/tweets` POST attempt。缺失 run、bundle 无法读取、truncation 或
时间断档都会使历史结论不可证明并判定 RC4 失败，绝不能用空 Observe snapshot 替代。

必须使用每个 Session shutdown 后的 `/runs` response 建立 final run inventory。每行记录
`session_id`、`run_id`、开始/结束时间、manifest SHA-256 与 bundle SHA-256；每个 run 都必须有
匹配的 manifest，以及可读、未截断的脱敏 bundle。逐 run 匹配结果与汇总结果都要保存，
`x_publish_request`、`x_published`、`POST /tweets` 三项汇总计数必须全部为零。下载 manifest 或
bundle 期间若 `/runs` 集合发生变化，必须刷新 inventory 并重新扫描，直到集合稳定。最终
coverage end 必须不早于 `TB-absent`。

Download the redacted log bundle for every run in both Sessions. The evidence bundle must contain:

下载两个 Session 中每一个 run 的 redacted log bundle。证据包必须包含：

- RC4 commit and tag resolution, all four `Tpkg-pre/post` values, `T0`, and Environment Profile name;
- observability preflight time, probe Session/run IDs, four HTTP statuses, health response, run
  manifest, and downloaded bundle SHA-256;
- `Tidentity`, the non-secret NyxID service slug, and the redacted `/users/me` username result;
- `Tsession-zero`, its Session registry JSON, and proof that `#135/#136`,
  `#140/#141/#142/#143`, and Trigger `#144` stayed outside RC4;
- Trigger A/B URLs, Session IDs, registration comments, config hashes, `TA-owner`/`TB-owner`,
  `TA-absent`/`TB-absent` registry and `404` evidence, and each Session's final live Observe JSON;
- shutdown-refreshed, stable A/B run inventories, every manifest and redacted bundle with its digest,
  and a `T0`-bounded historical scan whose coverage ends at or after `TB-absent` and proves zero X
  queue delivery and zero `/tweets` POST;
- four source-copy mappings and URLs, with a diff of only the intended controls;
- two Proposal URLs and complete revision/lineage/Proposal-digest/Content-digest history;
- two Content URLs and their immutable approval/content digests;
- stale approval and both negative-case responses;
- scoped counts proving two Proposals, two Contents, and zero Schedules.

- RC4 commit/tag resolution、四个 `Tpkg-pre/post` 值、`T0` 与 Environment Profile 名称；
- observability preflight 时间、probe Session/run ID、四个 HTTP status、health response、run
  manifest 与下载 bundle 的 SHA-256；
- `Tidentity`、非敏感的 NyxID service slug 与脱敏 `/users/me` username 结果；
- `Tsession-zero`、对应 Session registry JSON，以及 `#135/#136`、`#140/#141/#142/#143` 与
  Trigger `#144` 始终未进入 RC4 的证据；
- Trigger A/B URL、Session ID、registration comment、config hash、`TA-owner`/`TB-owner`、
  `TA-absent`/`TB-absent` registry 与 `404` 证据，以及每个 Session 最后一份 live Observe JSON；
- shutdown 后刷新且已稳定的 A/B run inventory、每个 manifest 与脱敏 bundle 及其 digest，
  以及按 `T0` 限定、coverage end 不早于 `TB-absent`、证明零 X queue delivery 与零
  `/tweets` POST 的历史扫描；
- 四份来源到副本的映射与 URL，以及只包含预期 controls 的 diff；
- 两个 Proposal URL 与完整 revision/lineage/Proposal-digest/Content-digest 历史；
- 两个 Content URL 及 immutable approval/content digest；
- stale approval 与两条负测的响应；
- 按 RC4 scope 统计的两个 Proposal、两个 Content 与零 Schedule。

Use the new fixture Issue numbers, `signal:` lineage, Session IDs, and `T0` window for every count.
Never count the whole repository because historical Proposal, Content, and Schedule Issues exist.

所有统计都必须以新 fixture Issue number、`signal:` lineage、Session ID 与 `T0` 时间窗为边界，
不能做全 repo 计数，因为仓库中已经存在历史 Proposal、Content 与 Schedule Issue。

## Timeouts and failure handling / 超时与失败处理

If a hard timeout is reached, stop issuing review commands and fixture writes. Capture the current
Observe snapshot, redacted logs, Issue states, and Trace IDs. A retrying delivery may continue only
within the 20-minute window; any permanent dead letter, duplicate Proposal, unexpected Schedule,
account mismatch, or X queue delivery fails the release. Do not edit business fields or add a replay
comment to turn a timeout green.

达到 hard timeout 后，立即停止新的 review command 与 fixture write，保存当前 Observe snapshot、
redacted logs、Issue state 与 Trace ID。Retrying delivery 只允许在 20 分钟窗口内继续；任何
permanent dead letter、重复 Proposal、意外 Schedule、账号错配或 X queue delivery 都直接阻塞
发布。不得通过修改业务字段或添加 replay comment 把 timeout 人工变绿。

## Pass, cleanup, and rollback / 通过、清理与回滚

RC4 passes only when all criteria below hold:

RC4 只有在以下条件全部成立时才通过：

| Gate / 门禁 | Expected result / 预期结果 |
|---|---|
| Observability preflight | Health, run list, manifest, and log bundle are readable before any RC4 GitHub write |
| Account identity | Redacted NyxID `/users/me` result is exactly `hzz780` for the recorded service slug |
| Official package drift | `Tpkg-pre-A/post-A/pre-B/post-B` all equal the pinned SHA |
| Historical isolation | RC2/RC3 fixtures and Trigger `#144` remain untouched and outside the route |
| Lane isolation | `Tsession-zero`, `TA-absent`, and `TB-absent` have zero active owner; `TA-owner`/`TB-owner` each have exactly their expected Session |
| COO aggregation | `S116/S117/S118 -> 1 W33 Proposal`; `S124 -> 1 W34 Proposal` |
| Session takeover | Trigger B reuses the same two Proposals; no duplicate revision or artifact |
| Human review | request-changes creates W33 r2; stale r1 approval fails; two current approvals succeed |
| Materialization | exactly two immutable approved Content Issues and zero Schedule Issues |
| Lifecycle | four Signals, two Proposals, and two Contents close without manual replay |
| Negative cases | both stay Open in triage and create no downstream artifact |
| X isolation | Stable inventories cover A through `TA-absent` and B through `TB-absent`; the aggregate history has zero publish request, receipt, and `/tweets` POST |
| Runtime health | final live Observe snapshots prove quiescence; shutdown registry/`404` evidence separately proves runtime absence |

Phase E closes Trigger B and verifies its runtime disappears before RC4 can pass. Keep generated v2
Issues as immutable acceptance evidence. Do not restore `auto-x-hzz780` to `#135/#136`,
`#140/#141/#142/#143`, or the negative fixtures until a future owner explicitly chooses to route
them; restoring it implicitly would feed historical work into the next Session.

Phase E 会在 RC4 可以判定通过前关闭 Trigger B 并确认 runtime 消失。生成的 v2 Issue 作为
immutable 验收证据保留。不要自动把 `auto-x-hzz780` 恢复给 `#135/#136`、
`#140/#141/#142/#143` 或本轮负测；
否则历史工作会被下一条 Session 隐式接管，只有未来 owner 明确决定重新路由时才能恢复。

Rollback closes the active RC4 Trigger, waits for runtime removal, removes the routing label from
all still-Open RC4 Issues, and freezes the generated v2 artifacts. It does not delete, rewrite, or
reopen any terminal artifact. Restoring v0.2.2 requires a separate Trigger after RC4 is fully stopped;
v1 and v2 must never share the same active label.

回滚时关闭 active RC4 Trigger，等待 runtime 删除，从所有仍 Open 的 RC4 Issue 移除 routing
label，并冻结已生成的 v2 artifact；不删除、不改写、不 reopen 任何 terminal artifact。只有在
RC4 完全停止后才能另建 Trigger 恢复 v0.2.2，v1 与 v2 禁止共用同一个 active label。

A live `hzz780` canary remains a separate approval after the exact account, tweet text, Content
digest, Schedule body, Profile, and execution time are shown. Shadow completion never enables X
writes automatically.

真实 `hzz780` Canary 仍需单独批准，并先展示确切账号、tweet text、Content digest、Schedule
body、Profile 与执行时间。Shadow 通过绝不会自动开启 X write。

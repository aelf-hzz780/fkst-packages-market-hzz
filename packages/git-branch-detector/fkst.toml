kind = "package"
name = "git-branch-detector"
persistence_class = "stateless_adapter"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "workflow_internal", "testkit", "testkit_internal", "forge"]

[conformance]
pack = "conformance/pack.toml"

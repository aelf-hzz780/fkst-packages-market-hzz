kind = "package"
name = "lark-approval"
persistence_class = "stateless_adapter"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit"]

[conformance]
pack = "conformance/pack.toml"

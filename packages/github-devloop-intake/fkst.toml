kind = "package.composed"
name = "github-devloop-intake"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "testkit_internal", "forge", "devloop"]

[event_deps]
packages = ["github-proxy"]

[conformance]
function = "core.saga_conformance_errors"

kind = "package.composed"
name = "github-devloop-integration"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "workflow_internal", "testkit", "testkit_internal", "forge", "devloop"]

[event_deps]
packages = ["github-proxy", "github-devloop", "github-devloop-pr"]

[conformance]
function = "core.saga_conformance_errors"

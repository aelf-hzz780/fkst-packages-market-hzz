kind = "package.composed"
name = "github-autochrono"
persistence_class = "composed_judgment_pipeline"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "testkit_internal"]

[event_deps]
packages = ["github-proxy", "autochrono"]
